import * as React from 'react';

const Title: React.StatelessComponent<{}> = () => (
  <label
    htmlFor='BulkActionPanel-selector-top'
    className='screen-reader-text'
  >
    Select bulk action
  </label>
);

export default Title;
